# =============================================================================
# TreadForm server (PRD-1 ~ 4 통합 의존성)
# =============================================================================
# 개별 패키지 버전은 현재 개발 환경(2026-05) 검증 기준. 마이너 업데이트는 호환
# 되지만 mediapipe 는 0.10.35 이상이 필수 (Tasks API + heavy 모델 자동 다운로드).

# ─────────── 분석 파이프라인 (PRD-1, 2, 3, 8) ───────────
mediapipe>=0.10.35
opencv-python>=4.10
numpy>=2.0
scipy>=1.13
pandas>=2.2
pillow>=10.0

# ─────────── API 레이어 (PRD-4) ───────────
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9   # multipart/form-data 영상 업로드
pydantic>=2.6

# ─────────── 테스트 ───────────
pytest>=8.0
httpx>=0.27               # FastAPI TestClient 의존성
